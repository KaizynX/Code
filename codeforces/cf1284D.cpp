#include <bits/stdc++.h>

using namespace std;

const int N = 1e5+7;

int n;
int sa[N], ea[N], sb[N], eb[N];

int main()
{
    cin >> n;
    for (int i = 1; i <= n; ++i) {
        cin >> sa[i] >> ea[i] >> sb[i] >> eb[i];
    }
    return 0;
}
